{{- if and .Values.aspire.enabled .Values.aspire.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "controlr.aspire.fullname" . }}
  labels:
    {{- include "controlr.aspire.labels" . | nindent 4 }}
  {{- with .Values.aspire.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.aspire.ingress.className }}
  ingressClassName: {{ .Values.aspire.ingress.className }}
  {{- end }}
  {{- if .Values.aspire.ingress.tls }}
  tls:
    {{- range .Values.aspire.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.aspire.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "controlr.aspire.fullname" $ }}
                port:
                  number: {{ $.Values.aspire.service.frontendPort }}
          {{- end }}
    {{- end }}
{{- end }}
